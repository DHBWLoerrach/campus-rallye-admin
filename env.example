# local user db
SQLITE_DB_PATH=local-users.db

# supabase url (client-side; also used server-side if SUPABASE_URL is not set)
NEXT_PUBLIC_SUPABASE_URL=
# optional server-side override (e.g. different internal network host)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_JWT_JWK=

# keycloak / oauth2-proxy
KEYCLOAK_ISSUER=http://localhost:8080/realms/dhbw
# client_id used for azp check
KEYCLOAK_AUDIENCE=campusrallye

# temporary access for users without 'staff' role (comma-separated emails)
# ALLOWED_EMAILS=user1@example.com,user2@example.com
